source "https://rubygems.org"

# Matches what GitHub Pages runs, so a local build and the CI build agree
# with production.
gem "github-pages", group: :jekyll_plugins

# Link checking in CI only.
gem "html-proofer", "~> 5.0", group: :development

# Required on Ruby 3.4+.
gem "csv"
gem "base64"
gem "bigdecimal"
